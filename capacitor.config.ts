import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "org.minefly.frontend",
  appName: "minefly",
  webDir: "out",
  bundledWebRuntime: false,
};

export default config;
